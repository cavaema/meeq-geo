# =============================================================================
# MEEQ-GEO - Variabili d'ambiente
# =============================================================================
# Multi-venue: il primo locale (id=1) viene creato da queste variabili al primo
# avvio se la tabella venues è vuota. Gli altri locali si aggiungono dall'admin
# (CRUD) oppure inserendo righe nella tabella venues.
# =============================================================================

PORT=3000

# -----------------------------------------------------------------------------
# Primo locale (venue id=1) - usato solo se la tabella venues è vuota
# -----------------------------------------------------------------------------
VENUE_NAME=Il nostro locale
VENUE_LOGO_URL=
VENUE_MENU_URL=

# Geolocalizzazione (disconnessione automatica quando esci dal locale)
VENUE_LATITUDE=
VENUE_LONGITUDE=
VENUE_RADIUS_METERS=80

# -----------------------------------------------------------------------------
# Server centrale (meeq-central)
# -----------------------------------------------------------------------------
CENTRAL_SERVER_URL=http://128.140.84.82:3001

# API Key per il locale 1 (da meeq-central admin). Per gli altri locali, 
# si imposta central_api_key nella tabella venues (via admin).
VENUE_API_KEY=
USE_CENTRAL_SERVER=true

# -----------------------------------------------------------------------------
# Admin (dashboard /admin)
# -----------------------------------------------------------------------------
ADMIN_USERNAME=admin
ADMIN_PASSWORD=meeq2024

# -----------------------------------------------------------------------------
# Opzionale: CORS per PWA (es. se l'app è su dominio diverso)
# Esempio: PWA_ALLOWED_ORIGINS=https://geo.meeq.it,https://app.meeq.it
# -----------------------------------------------------------------------------
# PWA_ALLOWED_ORIGINS=
